{% extends 'base.html.twig' %}

{% block body %}
    <h1>Themeofficiel</h1>

    <table>
        <tbody>
            <tr>
                <th>Theme</th>
                <td>{{ themeOfficiel.theme }}</td>
            </tr>
            <tr>
                <th>Users</th>
                <td>{{ themeOfficiel.usersThemes }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('themeofficiel_index') }}">Retour à la liste</a>
        </li>
        <li>
            <a href="{{ path('themeofficiel_edit', { 'id': themeOfficiel.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
