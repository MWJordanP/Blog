{% extends 'base.html.twig' %}

{% block body %}
    <h1>Signalement</h1>

    <table>
        <tbody>
            <tr>
            </tr>
            <tr>
                <th>Cause</th>
                <td>{{ signalement.cause }}</td>
            </tr>
            <tr>
                <th>User</th>
                <td>{{ signalement.userSignalement }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('signalement_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('signalement_edit', { 'id': signalement.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
